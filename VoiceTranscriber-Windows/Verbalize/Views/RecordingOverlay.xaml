<Window x:Class="Verbalize.Views.RecordingOverlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Recording" Width="320" Height="140"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" Topmost="True"
        ShowInTaskbar="False" ResizeMode="NoResize"
        WindowStartupLocation="Manual">

    <Border Background="#E6181825" CornerRadius="16" Padding="20,16"
            BorderBrush="#40FFFFFF" BorderThickness="1">
        <StackPanel>
            <!-- Header -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <!-- Pulsing red dot -->
                <Ellipse x:Name="RecordingDot" Width="10" Height="10" Fill="#EF4444" Margin="0,0,8,0">
                    <Ellipse.Triggers>
                        <EventTrigger RoutedEvent="Loaded">
                            <BeginStoryboard>
                                <Storyboard RepeatBehavior="Forever">
                                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                     From="1" To="0.3" Duration="0:0:0.8"
                                                     AutoReverse="True" />
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </Ellipse.Triggers>
                </Ellipse>
                <TextBlock Text="Listening" FontSize="15" FontWeight="SemiBold" Foreground="White" />
                <TextBlock x:Name="DotsText" Text="..." FontSize="15" Foreground="#80FFFFFF" Margin="2,0,0,0" />

                <!-- Duration badge -->
                <Border Background="#30FFFFFF" CornerRadius="10" Padding="8,2" Margin="12,0,0,0">
                    <TextBlock x:Name="DurationText" Text="0.0s" FontSize="12" Foreground="#CCFFFFFF"
                               FontFamily="Cascadia Code, Consolas" />
                </Border>
            </StackPanel>

            <!-- Waveform bars -->
            <Canvas x:Name="WaveformCanvas" Height="36" Margin="0,0,0,8" />

            <!-- Cancel hint -->
            <TextBlock Text="Press Esc to cancel" FontSize="11" Foreground="#50FFFFFF"
                       HorizontalAlignment="Center" />
        </StackPanel>
    </Border>
</Window>
